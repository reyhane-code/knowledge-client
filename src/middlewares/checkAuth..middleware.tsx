// import { RouteComponentProps, RedirectFunction } from 'react-router';

// const checkAuthMiddleware = (isAuthenticated: boolean, authenticationPath: string) => {
//   return (props: RouteComponentProps, replace: RedirectFunction) => {
//     if (!isAuthenticated) {
//       replace(authenticationPath);
//     }
//   };
// };